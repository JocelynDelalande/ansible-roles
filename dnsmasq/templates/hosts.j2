127.0.0.1	localhost

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

# Split-horizon DNS

# myself
{{ dmz_ip4_addr }}	{{ ansible_fqdn }} {{ ansible_hostname }}
# and the others
{% for host in dmz_hosts %}
{{ host.ip4 }}	{% for name in host.names %}{{ name }} {% endfor %}

{% endfor %}
